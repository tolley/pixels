claude work
┌────────────────────┬───────────────────────────────────────────────────────────────────────────────────────────────┐
│        File        │                                            Change                                             │
├────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────┤
│ www/db.php         │ Added email_verified column + migration guard + verify_tokens table                           │
├────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────┤
│ www/auth.php       │ New — handles signup, login, logout, me, resend, verify                                       │
├────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────┤
│ www/grid.php       │ PHP session start, auth modal overlay (login/signup tabs), user+logout in toolbar, toast      │
│                    │ notifications                                                                                 │
├────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────┤
│ docker-compose.yml │ Added MailHog service + APP_URL env var                                                       │
├────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────┤
│ php/Dockerfile     │ Installs msmtp, writes /etc/msmtprc pointing to MailHog, sets PHP sendmail_path               │
└────────────────────┴───────────────────────────────────────────────────────────────────────────────────────────────┘

Flow:
1. User opens grid.php → auth modal blocks the UI until logged in
2. Sign Up → email sent via MailHog → user clicks verify link → auto-logged in, redirected back with "Email verified — welcome!" toast
3. Login → requires verified email; if not verified, a "Resend verification email" link appears
4. Logout → session destroyed, modal reappears

┌────────────────┬─────────────────────────────────────────────────────────────────────────────────────────┐
│      File      │                                         Purpose                                         │
├────────────────┼─────────────────────────────────────────────────────────────────────────────────────────┤
│ www/jwt.php    │ HS256 JWT encode/decode, httpOnly cookie helpers                                        │
├────────────────┼─────────────────────────────────────────────────────────────────────────────────────────┤
│ www/login.php  │ Standalone login page — fetches auth.php?action=login, redirects to grid.php on success │
├────────────────┼─────────────────────────────────────────────────────────────────────────────────────────┤
│ www/signup.php │ Standalone signup page — fetches auth.php?action=signup, shows "check email" on success │
├────────────────┼─────────────────────────────────────────────────────────────────────────────────────────┤
│ www/logout.php │ Clears the JWT cookie, redirects to login.php                                           │
└────────────────┴─────────────────────────────────────────────────────────────────────────────────────────┘

Modified files:


- www/auth.php — removed PHP sessions entirely; login/verify now call jwtSetCookie(), logout calls jwtClearCookie(); verify errors redirect to login.php?error=...
- www/grid.php — replaced the session modal with a 4-line PHP JWT gate at the top; username is PHP-rendered server-side; logout is a plain <a href="logout.php"> link; all auth JS removed
- docker-compose.yml — added JWT_SECRET env var

Flow:
1. Go to grid.php → redirected to login.php if no valid JWT cookie
2. Login → JWT set as httpOnly SameSite=Strict cookie → redirect to grid.php
3. Sign up → check email → click verify link → JWT set → redirect to grid.php?verified=1 → toast shown
4. Logout link → logout.php clears cookie → login.php


//////////////////////////////////////////////////////////////////
////////////////////// Social logins /////////////////////////////
//////////////////////////////////////////////////////////////////
Rebuild the container to pick up the Dockerfile changes: docker-compose up --build.

Files changed:
- db.php — auto-migrates users table: password becomes nullable, adds oauth_provider + oauth_subject columns with a unique index
- login.php / signup.php — show Google/Apple buttons when the env vars are set
- docker-compose.yml — placeholder vars with setup instructions
- oauth.php (new) — shared: stateless HMAC state (CSRF-safe without sessions), oauthFindOrCreateUser, username generation
- oauth_google.php (new) — Google OAuth start + callback
- oauth_apple.php (new) — Apple Sign In start + POST callback (includes ES256 JWT signing for Apple's client secret)

---
To activate Google:
1. Go to console.cloud.google.com → Credentials → Create OAuth 2.0 Client ID
2. Add http://localhost:8080/oauth_google.php?action=callback as an authorized redirect URI
3. Fill in GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET in docker-compose.yml

To activate Apple:
1. In Apple Developer → Certificates, Identifiers & Profiles: create a Services ID (your APPLE_CLIENT_ID) and a Sign In with Apple key
2. Add http://localhost:8080/oauth_apple.php as a return URL
3. Download the .p8 key file and base64-encode it: base64 -w 0 AuthKey_XXXXXX.p8
4. Fill in all four APPLE_* vars in docker-compose.yml

Account linking: If a user signs in via Google/Apple with an email that already exists from email/password signup, the OAuth identity is automatically linked to that account — no duplicate users.


/////////////////////////////////////////////////////////////////////////////////////
///////////////////////////////// Generated approval process ////////////////////////
/////////////////////////////////////////////////////////////////////////////////////

---
Database
- users.is_admin column (migration, defaults to 0)
- pending_pixels table: batches of user-submitted changes with status (pending/approved/rejected)

Auth flow (jwt.php, auth.php, oauth.php, oauth_google.php, oauth_apple.php)
- is_admin is now included in the JWT, propagated through all login paths (password, email verify, Google, Apple)

API (api.php)
- POST now requires a valid JWT and writes to pending_pixels (not pixels). color: null = an erase request
- DELETE now requires is_admin

Review API (review-api.php)
- Admin-only (403 otherwise)
- GET — returns pending batches with submitter, timestamp, coord sample, paint/erase counts, color swatches
- POST {action, batch_id} — approve applies pixels/erases to the live pixels table; reject simply marks them

Review page (review.php)
- Redirects non-admins to login
- Lists pending batches as cards: user, time, counts, color swatches, coordinates
- Approve/Reject buttons; card fades and disappears on action

Grid (grid.php)
- Submit now sends everything through a single POST (erases as color: null)
- "Submit" button shows a "Submitted for review" toast on success
- Admin users see a Review link in the toolbar

To promote the first admin user, run this directly in MySQL:
UPDATE users SET is_admin = 1 WHERE email = 'your@email.com';
After the user next logs in (fresh JWT), they'll see the Review link and have access to review.php.
